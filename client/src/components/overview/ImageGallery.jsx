import React from 'react';
import styled from 'styled-components';

import { GalleryWrapper } from '../../../dist/overviewStyling.js';

export const ImageGallery = (props) => {
  return (
    <GalleryWrapper>
      <div className="OverviewGallery"></div>
    </GalleryWrapper>
  )
}